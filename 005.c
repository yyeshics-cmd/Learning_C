#include <stdio.h>
int main(){
    //create variables
    int length = 4;
    int width = 6;
    int area;

    //calculate the area
    area = length * width;

    //print results
    printf("Length is: %d\n", length);
    printf("Width is: %d\n", width);
    printf("Area of the rectangle is: %d\n", area);
    return 0;
}